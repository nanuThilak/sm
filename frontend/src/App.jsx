import React, { useEffect, useState } from "react";

const App = () => {
  
  return (
    <div>
      <button onClick={handleClick}>click</button>
      <h1>{count}</h1>
    </div>
  );
};

export default App;
